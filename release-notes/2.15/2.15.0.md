# v1.15.0 — Mix detail verbeteringen + domein en taal gecorrigeerd

**Datum:** 2026-06-25
**Type:** Minor
**Branch:** `feature/mix-detail-page-improvements`

## Wijzigingen

### src/app/luister/mix/[slug]/page.tsx

- **Bugfix:** mixes met `ignore: true` zijn nu correct uitgesloten van zowel de detailpagina als `generateStaticParams` (geen 404 meer nodig, pagina wordt niet gegenereerd)
- Subgenre wordt getoond onder de h1 (indien gevuld)
- Datum wordt getoond onder de h1 (dag maand jaar, indien `jaar` gevuld)
- JSON-LD: `datePublished` toegevoegd indien `date` aanwezig
- JSON-LD: `genre` gebruikt nu `mix.subgenre || mix.genre`
- Alle hardcoded `djcylow.nl` URL's vervangen door `djcylow.com`

### src/app/layout.tsx

- `lang="nl"` → `lang="en"` (website is Engelstalig)

### scripts/add-mix.js

- Claude Haiku beschrijvingsprompt omgezet naar Engels

### src/app/luister/mix/README.md (nieuw)

- Documenteert de relatie tussen `src/data/mixes/` en de mix detail pagina
- Overzicht van alle JSON-velden en waar ze op de pagina worden gebruikt

### CLAUDE.md

- Taalbeleid vastgelegd: alle user-facing tekst in het Engels
- Domein gecorrigeerd naar `djcylow.com`
- Mix description-regel: "Dutch" → "English"

### src/data/mixes/old_json/ (verwijderd)

- Verouderde backup-map verwijderd uit de repository

## Rollback

```sh
git checkout v1.14.4
```
